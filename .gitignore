# Session tokens (sid/ecode/partner) — never commit
session.json

# Python
.venv/
__pycache__/
*.pyc

# Built binaries
bridge/avent-webrtc-bridge
web/babymonitor-web
/.tuya-data

# Swift / macOS app build artifacts
app/.build/
app/build/
DerivedData/
app/helpers/
