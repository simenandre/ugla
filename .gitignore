# Swift / macOS app build artifacts
app/.build/
app/build/
app/helpers/
DerivedData/

# Release output
dist/

# Bridge build output (if built in-tree)
bridge/avent-webrtc-bridge

# Code-signing artifacts (never commit)
*.cer
*.p12
*.certSigningRequest
*.key
